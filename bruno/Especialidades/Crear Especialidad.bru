meta {
  name: Crear Especialidad
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/especialidades
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "nombre": "Pediatría"
  }
}
